export interface ConfigModel {

    table: string
    master_circle: string;
    parent_circle: string;
    children_circle: string;
    parent_size: string;
    children_size: string;
    parent_tooltip: string;
    children_tooltip: string
}